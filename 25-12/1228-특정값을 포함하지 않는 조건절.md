
## 1단계 - 'boring'값 들어가면 제외시키기
### 1. 표준 비교 연산자 (`<>`)
- **작동 방식**: 컬럼 내 각 레코드를 순회하며 `'a'`와 일치하지 않는 데이터만 `TRUE`로 판정하여 추출
- **특징**: ISO SQL 표준 규격으로 모든 DBMS에서 호환 가능
- **문법**:
SELECT * FROM 테이블명 WHERE 컬럼명 <> 'a';

### 2. 대체 비교 연산자 (`!=`)
- **작동 방식**: `<>` 연산자와 동일한 메커니즘으로 작동하며, 지정된 값과 다른 행을 필터링
- **특징**: C 계열 언어와 유사한 문법으로 직관적이며 대부분의 DBMS 지원
- **문법**:
SELECT * FROM 테이블명 WHERE 컬럼명 != 'a';

#### 3. 논리 부정 연산자 (`NOT`)
- **작동 방식**: 컬럼 값이 `'a'`와 일치하는(`=`) 조건의 논리 결과값을 반전시켜 처리
- **문법**:
SELECT * FROM 테이블명 WHERE NOT 컬럼명 = 'a';


> 개인적으로 Python과 유사한 2번방식이 익숙함
> 성능은 거의 동일하다고 하니까 2번방식으로 쭉 써야겠다.
```
SELECT *
FROM Cinema c
WHERE c.description != "boring";

<result>
| id | movie      | description | rating |
| -- | ---------- | ----------- | ------ |
| 1  | War        | great 3D    | 8.9    |
| 2  | Science    | fiction     | 8.5    |
| 4  | Ice song   | Fantacy     | 8.6    |
| 5  | House card | Interesting | 9.1    |
```


## 2단계 - 홀수인 id 필터링
> 파이썬이랑 비슷  
> c.id % 2 == 1 으로 했다가 틀림.  
> c.id % 2 = 1 비교연산자는 그냥 =(등호) 사용  

```
SELECT *
FROM Cinema c
WHERE c.description != "boring"
AND c.id%2 = 1;


| id | movie      | description | rating |
| -- | ---------- | ----------- | ------ |
| 1  | War        | great 3D    | 8.9    |
| 5  | House card | Interesting | 9.1    |
```



## 3단계 - 정렬(내림차순)

- 최종 정답쿼리 
```
SELECT *
FROM Cinema c
WHERE c.description != "boring"
AND c.id%2 = 1
ORDER BY c.rating DESC;

<result>
| id | movie      | description | rating |
| -- | ---------- | ----------- | ------ |
| 5  | House card | Interesting | 9.1    |
| 1  | War        | great 3D    | 8.9    |
```

# [leet] 620. Not Boring Movies

```
Table: Cinema

+----------------+----------+
| Column Name    | Type     |
+----------------+----------+
| id             | int      |
| movie          | varchar  |
| description    | varchar  |
| rating         | float    |
+----------------+----------+
id is the primary key (column with unique values) for this table.
Each row contains information about the name of a movie, its genre, and its rating.
rating is a 2 decimal places float in the range [0, 10]
 

Write a solution to report the movies with an odd-numbered ID and a description that is not "boring".

Return the result table ordered by rating in descending order.

The result format is in the following example.

 

Example 1:

Input: 
Cinema table:
+----+------------+-------------+--------+
| id | movie      | description | rating |
+----+------------+-------------+--------+
| 1  | War        | great 3D    | 8.9    |
| 2  | Science    | fiction     | 8.5    |
| 3  | irish      | boring      | 6.2    |
| 4  | Ice song   | Fantacy     | 8.6    |
| 5  | House card | Interesting | 9.1    |
+----+------------+-------------+--------+
Output: 
+----+------------+-------------+--------+
| id | movie      | description | rating |
+----+------------+-------------+--------+
| 5  | House card | Interesting | 9.1    |
| 1  | War        | great 3D    | 8.9    |
+----+------------+-------------+--------+
Explanation: 
We have three movies with odd-numbered IDs: 1, 3, and 5. The movie with ID = 3 is boring so we do not include it in the answer.
```
